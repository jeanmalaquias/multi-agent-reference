apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-multi-agent
  labels:
    app: {{ .Release.Name }}-multi-agent
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ .Release.Name }}-multi-agent
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
